import React from 'react';

import { LoaderContainer, LoaderContent } from './Loader.styles';

const Loader = () => {
  return (
    <LoaderContainer>
      <LoaderContent />
    </LoaderContainer>
  );
};

export default Loader;
